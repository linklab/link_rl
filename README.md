# link_rl
reinforcement learning lab for link@koreatech

# pip install
pip install numpy
pip install pandas
pip install scikit-learn
pip install matplotlib
pip install imageio
pip install graphviz
pip install torch torchvision torchaudio
pip install torchviz
pip install wandb
pip install gymnasium[all]
pip install gymnasium[other]
pip install flappy-bird-gymnasium
pip install ale-py

# pip install all at once
pip install numpy pandas scikit-learn matplotlib imageio graphviz torch torchvision torchaudio torchviz wandb gymnasium[all] gymnasium[other] flappy-bird-gymnasium ale-py
